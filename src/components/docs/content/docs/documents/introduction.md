---
title: مستندات
summary: مستندات ایجاد کنید تا متن و نمودارها را برای تحلیل بلند ترکیب کنید. نمودارها را از سؤال‌ها و مدل‌ها اضافه کنید و متن را با Markdown فرمت کنید.
---

# مستندات

{% include plans-blockquote.html feature="Documents" %}

![مستند با نظر](./images/document-with-comment.png)

مستندات به شما امکان می‌دهند با ترکیب متن، نمودارها و محتوای دیگر با داده‌هایتان داستان بگویید.

## ایجاد یک مستند

1. روی دکمهٔ **+ New** در گوشهٔ بالا سمت راست کلیک کنید.
2. **Document** را انتخاب کنید.
3. کارهایتان را انجام دهید.
4. روی دکمهٔ **Save** کلیک کنید.

## فهرست کردن دستورات با `/`

در مستندتان `/` را تایپ کنید تا دستورات در دسترس را ببینید:

- **Ask Metabot** برای چت با [Metabot](../ai/metabot.md) در مستند.
- **Chart** برای درج یک جدول یا نمودار از یک سؤال یا مدل موجود. نمی‌توانید داشبوردها یا مستندات دیگر را اضافه کنید، اما می‌توانید به آن‌ها اشاره کنید (به بخش بعدی مراجعه کنید).
- **Link**. می‌توانید به آیتم‌های دیگر در متابیس خود اشاره کنید.

## اشاره به آیتم‌ها با `@`

می‌توانید `@` را تایپ کنید تا به آیتم‌های متابیس خود اشاره کنید. (اشاره‌ها یک آیکون کوچک فانتزی کنارشان دارند).

## اضافه کردن نمودارها به مستندات

می‌توانید [سؤال‌ها](../questions/start.md) و [مدل‌ها](../data-modeling/models.md) را به یک مستند اضافه کنید.

### اضافه کردن یک نمودار آن را در مستند کپی می‌کند

وقتی یک نمودار از یک کلکسیون اضافه می‌کنید، متابیس یک کپی در مستند ایجاد می‌کند. سؤال اصلی در کلکسیون باقی می‌ماند. تغییرات در یک کپی روی کپی دیگر تأثیر نمی‌گذارد؛ آن‌ها مستقل هستند.

با این حال، اگر کپی مستند را حذف کنید، آن کپی به‌طور دائمی حذف می‌شود. نمی‌توان آن را از [سطل زباله](../exploration-and-organization/delete-and-restore.md) بازیابی کرد.

### نمودارهای تولیدشده توسط Metabot فقط در مستند وجود دارند

اگر [Metabot](../ai/metabot.md) یک نمودار تولید کند، متابیس آن سؤال را در خود مستند ذخیره می‌کند. اگر آن را حذف کنید، برای همیشه از بین می‌رود. نمی‌توانید سؤال را از سطل زباله بازیابی کنید.

## ویرایش نمودارها

برای ویرایش یک نمودار، روی منوی **سه‌نقطه** در گوشهٔ بالا سمت راست نمودار کلیک کنید. می‌توانید:

- ویژوالیزیشن را ویرایش کنید
- کوئری را ویرایش کنید
- نمودار را با یک سؤال یا مدل دیگر جایگزین کنید
- [نتایج را دانلود کنید](../questions/exporting-results.md#exporting-results-from-document-cards)

ویرایش‌ها فقط نمودار در مستند را تحت تأثیر قرار می‌دهند. اگر نمودار را از یک کلکسیون اضافه کرده‌اید، متابیس یک کپی از نمودار ایجاد کرده است. هر تغییری که در نمودار در مستند ایجاد شود روی سؤال اصلی تأثیر نمی‌گذارد.

## مرتب‌سازی نمودارها

![چیدمان مستند](./images/document-layout.png)

برای مرتب‌سازی مجدد نمودارها، به سادگی روی نمودار کلیک کنید و نگه دارید و آن را به جایی که می‌خواهید در مستند بکشید.

می‌توانید تا سه نمودار را کنار هم قرار دهید (بیش از سه نمودار کنار هم شلوغ می‌شود). برای تغییر اندازه نمودارها، می‌توانید روی لبه‌ها کلیک کنید تا اندازه‌شان را تنظیم کنید.

وقتی چندین نمودار در همان ردیف وجود دارند، می‌توانید آن‌ها را به صورت افقی تغییر اندازه دهید با کلیک بین نمودارها و کشیدن به چپ یا راست برای تغییر اندازه.

برای تغییر اندازه ارتفاع یک نمودار، روی پایین نمودار کلیک کنید و نمودار را بالا یا پایین تغییر اندازه دهید.

فعلاً فقط می‌توانید نمودارها را کنار هم قرار دهید (نه عناصر متنی).

بعد از مرتب‌سازی نمودارهایتان، تغییراتتان را ذخیره کنید!

## فرمت‌کردن متن

مستندات از syntax Markdown برای موارد زیر پشتیبانی می‌کنند:

- عنوان‌ها: `#`
- تصاویر: از syntax تصویر Markdown استفاده کنید: `![متن تصویر](لینک تصویر)`. تصویر باید خط خودش را در مستند شروع کند، و ممکن است نیاز داشته باشید پرانتز بسته را تایپ کنید تا متابیس بداند تصویر را رندر کند. لینک تصویر باید یک URL معتبر باشد که متابیس شما بتواند به آن دسترسی داشته باشد.
- نقل‌قول‌های بلوکی: `> این یک نقل‌قول بلوکی است`.
- بلوک‌های کد: با سه backtick (\`\`\`) بپیچید. هایلایت syntax هنوز پشتیبانی نمی‌شود.

همچنین می‌توانید با تایپ کردن `/` به گزینه‌های فرمت‌کردن دسترسی داشته باشید.

## لینک‌ها

اگر یک لینک را در یک مستند جای‌گذاری کنید، متابیس آن را به یک لینک قابل کلیک تبدیل می‌کند. فعلاً لینک‌های Markdown پشتیبانی نمی‌شوند و نمی‌توانید متن لینک را تغییر دهید.

## نظر دادن روی مستندات

برای نظر دادن روی یک عنصر متنی، روی حباب چت کنار بخش کلیک کنید. فقط می‌توانید روی کل بخش‌ها نظر دهید؛ نمی‌توانید متن را هایلایت کنید تا روی آن نظر دهید.

برای نظر دادن روی یک نمودار:

1. روی نمودار hover کنید.
2. روی منوی سه‌نقطه کلیک کنید.
3. **Comment** را انتخاب کنید.

می‌توانید:

- به نظرات با ایموجی‌ها واکنش نشان دهید (که مثل تصاویری هستند که خود را کلمات جا می‌زنند). به‌عنوان مثال، این یک گندولا است: 🚠.
- به آیتم‌ها یا افراد دیگر در متابیس خود اشاره کنید (`@` را تایپ کنید و سپس جستجو کنید که می‌خواهید به چه کسی/چه چیزی اشاره کنید).
- به یک نظر لینک دهید. روی منوی **سه‌نقطه** کلیک کنید و **Copy link** را انتخاب کنید.
- نظرات را حل کنید. نظرات حل‌شده در یک تب جداگانه ظاهر می‌شوند.

### اعلان‌های نظر

اگر کسی در یک thread به شما اشاره کند، یا به یک نظر در threadی که در آن هستید پاسخ دهد، متابیس یک ایمیل برای اطلاع‌رسانی به شما ارسال می‌کند.

فعلاً هیچ مکان مرکزی برای مشاهدهٔ همهٔ threadها و اشاره‌های شما وجود ندارد، و هیچ راهی برای غیرفعال کردن اعلان‌های ایمیل برای نظرات وجود ندارد.

### حذف نظرات

اگر هرگز نمی‌خواهید یک نظر را دوباره ببینید:

1. نظر را حل کنید.
2. آن را در تب Resolved پیدا کنید.
3. روی منوی **سه‌نقطه** کلیک کنید.
4. **Delete** را انتخاب کنید.

این حذف دائمی است.

## چاپ مستندات

برای چاپ یک مستند، روی منوی **سه‌نقطه** کلیک کنید و **Print document** را انتخاب کنید.

## اشتراک‌گذاری مستندات

ادمین‌ها می‌توانند [لینک‌های عمومی](../embedding/public-links.md#create-a-public-link-for-a-document) برای اشتراک‌گذاری مستندات با هر کسی ایجاد کنند.

## چه زمانی از یک مستند در مقابل یک داشبورد استفاده کنیم

مستندات و داشبوردها هر دو موارد استفادهٔ خود را دارند.

### ترجیح مستندات برای اکتشاف و روایت

مستندات برای موارد زیر بهترین کار را می‌کنند:

- گفتن یک داستان با داده
- باز کردن تحلیل برای نظرات
- تحلیل اکتشافی

### استفاده از داشبوردها برای ردیابی متریک‌ها

داشبوردها برای موارد زیر بهترین کار را می‌کنند:

- تحلیل منظم. اعدادی که می‌خواهید هر روز، هفته، یا ماه نظارت کنید.
- فیلتر کردن نتایج در چندین نمودار.
- کنترل بیشتر بر ارائه.

## مجوزهای مستند

اینکه کسی می‌تواند یک مستند و داده‌هایش را مشاهده یا ویرایش کند بستگی به مجوزهای کلکسیون و دادهٔ آن شخص دارد. به [مجوزها](../permissions/introduction.md) مراجعه کنید.
